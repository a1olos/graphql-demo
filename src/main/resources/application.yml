spring:
    datasource:
        dynamic:
            primary: meta
            datasource:
                meta:
                    url: jdbc:mysql://127.0.0.1:3306/graphql-demo?characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
                    username: root
                    password: 123456
                    driver-class-name: com.mysql.jdbc.Driver
    graphql:
        graphiql:
            enabled: true
            path: /graphql-page
server:
    port: 8080